# 项目约定

- 当前为项目框架。没有实现和验证的功能不得描述为完成；不得填入虚构实验数字。
- 按 docs/project-plan.md 逐步实施，以正确性、后端实际可用性和可复现性为先。
- 核心求解采用单进程、实数双精度。数值依赖延迟导入，保持 --help 和 --dry-run 在无 PETSc 环境可用。
- 新增数值代码后，验证离散已知解、连续解析解及相应故障案例，不以运行返回代替数值正确性。
- Markdown 块级公式统一用独占行的 $$ 开始和结束，前后留空行。
- 用户自行上传 Git；未经请求不推送仓库或把未完成的项目写成简历成果。
- 如未来出现 .codegraph/ 索引，理解或定位代码时先使用 CodeGraph；没有索引时不自行建立。
